{
  "name": "praxa-sdk-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source Praxa SDK, CLI, and MCP contracts.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "node --test tests/*.test.mjs",
    "check:boundary": "node scripts/check-public-boundary.mjs",
    "check:packages": "node scripts/check-packages.mjs",
    "verify": "npm run check:boundary && npm run build && npm test && npm run check:packages"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
